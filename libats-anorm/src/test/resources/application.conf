ats = {
  database = ${anorm-async.database}
  database = {
    jdbcurl = "jdbc:mariadb://localhost:3306/libats"
    jdbcurl = ${?DB_URL}
    user = "libats"
    password = "libats"
    database = "libats"
  }
}
